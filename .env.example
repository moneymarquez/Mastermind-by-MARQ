VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-side only — read by netlify/functions/claude.ts, never sent to the browser.
# Not prefixed with VITE_ on purpose: Vite would otherwise inline it into the client bundle.
ANTHROPIC_API_KEY=
