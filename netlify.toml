[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Single-page app: let React Router-style client-side navigation
# (and any deep link / reload) resolve to index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
